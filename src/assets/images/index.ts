/* IMAGES ENTRY
   ========================================================================== */

import Logo from "./logo.png";

const Png = {
  Logo,
};

const Images = {
  Png,
};

export default Images;
